apiVersion: v1
kind: Service
metadata:
  name: svc-admin-frontend
spec:
  selector:
    app: admin-frontend
  ports:
    - name: http-admin-frontend-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
